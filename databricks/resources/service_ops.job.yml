resources:
  jobs:
    service_ops_batch:
      name: service-ops-batch
      max_concurrent_runs: 1
      tasks:
        - task_key: bronze_to_silver_to_gold
          spark_python_task:
            python_file: ../notebooks/medallion_pipeline.py
          new_cluster:
            spark_version: 15.4.x-scala2.12
            node_type_id: Standard_DS3_v2
            num_workers: 1
            autotermination_minutes: 15
