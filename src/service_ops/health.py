"""Small, dependency-free smoke-check surface for Phase 0."""


def foundation_status() -> str:
    """Return the stable marker used by the Phase 0 import smoke test."""
    return "service-ops foundation ready"
