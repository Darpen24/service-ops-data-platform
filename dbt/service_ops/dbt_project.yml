name: service_ops
version: 1.0.0
config-version: 2
profile: service_ops
model-paths: [models]
macro-paths: [macros]
snapshot-paths: [snapshots]
test-paths: [tests]
target-path: target
clean-targets: [target, dbt_packages, logs]
models:
  service_ops:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
